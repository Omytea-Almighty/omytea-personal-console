# Claude entrypoint

<!-- OMYTEA_ORG_SYNC_CLAUDE_START -->
@AGENTS.md

Read [`ORG-SYNC.md`](ORG-SYNC.md) first. The organization identity, visibility,
fast-forward, public-release, hook, governance, and remote-SHA gates are binding
and fail closed. Managed commit/rewrite hooks queue; direct push is denied;
registered linked worktrees are supported. Never bypass with `--no-verify` or
`core.hooksPath`. `Adonyth/almighty` is excluded.
<!-- OMYTEA_ORG_SYNC_CLAUDE_END -->
